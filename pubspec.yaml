name: medilink_ai
description: "MediLink AI — AI-powered emergency hospital resource orchestration and predictive transfer engine."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_generative_ai: ^0.4.6
  provider: ^6.1.2
  shimmer: ^3.0.0
  intl: ^0.19.0
  csv: ^6.0.0
  path_provider: ^2.1.4
  share_plus: ^10.1.4
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
